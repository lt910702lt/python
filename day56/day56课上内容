内容回顾：
    1、外键的变种
        - 一对多或多对一
            多的要设置外键
        - 多对多
            创建第三张表，与另外两张表进行关联
        - 一对一
            有外键的字段设置unique
    2、表单查询
        select 字段1、字段2 from 表名 where id > 3 group by post having post='xxx' order by id desc limit 1;

今日内容：
    1、多表查询
    2、索引